# Dashboard JSON

Provisioned dashboards live here as `*.json`.

- Phase 1 — `user-actions.json` ("User Actions — Load Test")
- Phase 2 — `baseline-vs-candidate.json`, `release-trend.json`
- Phase 4 — `app-server-jvm.json`

Empty in Phase 0 (no application metrics yet). Grafana provisioning tolerates an
empty folder.
