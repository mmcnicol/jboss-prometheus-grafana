<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.jpg</groupId>
  <artifactId>jboss-prometheus-grafana</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>jboss-prometheus-grafana (reactor)</name>
  <description>
    Reference implementation: load-test metrics from a JBoss/JSF application into
    Prometheus and Grafana. Targets WildFly 26.1 (EAP 7.4 stand-in), Jakarta EE 8
    (javax.*), JDK 17.
  </description>

  <modules>
    <module>metrics-support/metrics-api</module>
    <module>metrics-support/metrics-micrometer</module>
    <module>metrics-support/metrics-prometheus</module>
    <module>metrics-support/metrics-servlet</module>
    <module>metrics-support/metrics-jaxrs</module>
    <module>demo-app/portal-web</module>
    <module>demo-app/service-a</module>
    <module>demo-app/service-b</module>
    <module>load/selenium-java</module>
  </modules>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Jakarta EE 8 / javax.* — matches WildFly 26.1 and EAP 7.4 -->
    <javaee.api.version>8.0.0</javaee.api.version>
    <primefaces.version>13.0.0</primefaces.version>

    <!-- Spike A: the two instrumentation backends under comparison -->
    <micrometer.version>1.13.6</micrometer.version>
    <prometheus.client.version>1.3.1</prometheus.client.version>

    <!-- test -->
    <junit.version>5.10.2</junit.version>
    <testng.version>7.10.2</testng.version>
    <selenium.version>4.25.0</selenium.version>
    <assertj.version>3.26.3</assertj.version>

    <!-- plugins -->
    <compiler.plugin.version>3.13.0</compiler.plugin.version>
    <war.plugin.version>3.4.0</war.plugin.version>
    <surefire.version>3.2.5</surefire.version>
    <failsafe.version>3.2.5</failsafe.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>jakarta.platform</groupId>
        <artifactId>jakarta.jakartaee-web-api</artifactId>
        <version>${javaee.api.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.primefaces</groupId>
        <artifactId>primefaces</artifactId>
        <version>${primefaces.version}</version>
      </dependency>

      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.testng</groupId>
        <artifactId>testng</artifactId>
        <version>${testng.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.seleniumhq.selenium</groupId>
        <artifactId>selenium-java</artifactId>
        <version>${selenium.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>${assertj.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-war-plugin</artifactId>
          <version>${war.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${failsafe.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
